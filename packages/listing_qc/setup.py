from setuptools import find_packages, setup

setup(
    name="listing-qc",
    version="0.1.0",
    description="Shared listing QC helpers for AquaVerve / GrovePop / AquaRides",
    packages=find_packages(),
    python_requires=">=3.10",
)
