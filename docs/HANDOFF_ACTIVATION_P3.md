# 交接任务书 #6：P3 续跑激活（两个开关）—— 授权例外文件

> 交接方：Claude（2026-07-14）
> 执行方：你（AI 执行模型）
> **⚠ 授权例外声明**：此前所有任务书（#1–#5）都禁止你改 `.env` 和碰守护进程。**本文件是唯一例外**——用户已明确授权你执行下面两个、且仅此两个敏感操作。除这两项外，一切禁令继续有效（禁 git commit、禁改其他配置、禁改代码、禁碰价格库存）。
> **执行时机**：作为最后一步，在人工队列降解（任务书 #5）的阶段 C apply 完成之后再做。两者 SKU 不冲突，但激活后 12:30 会自动跑，放最后最干净。

---

## 授权的两个操作（只此两项）

### 操作 1：向 `.env` 追加一行开关

**目的**：打开语义改写 12:30 调度任务的 live-write 双闸第二扇。

**安全规程（必须逐条遵守）**：
1. **只追加，不重写**：用追加方式在文件末尾加一行 `SEMANTIC_REWRITE_APPLY_ENABLED=1`，绝不重排/改写/删除任何既有行。
2. 追加前记录 `.env` 键数（当前 **20** 个键）；追加后必须是 **21** 个键，且原 20 个键（尤其 `EBAY_REFRESH_TOKEN` / `DAJIAN_API_SECRET` / `QWEN_API_KEY` / `NOTIFICATION_EMAIL_PASSWORD` 等凭证）**逐个仍在、值未变**。
3. **绝不打印 `.env` 的值**到日志/报告（只报键名与键数）。
4. 若 `.env` 已含 `SEMANTIC_REWRITE_APPLY_ENABLED`（任何值）→ 不追加，改为确认其为 `1`（若不是，就地改这一行的值为 `1`，不动其他行）。

### 操作 2：重启守护进程

**目的**：当前 PID 13628 启于 2026-07-13 21:48，早于 P3 代码落地，运行实例不含 `task_semantic_rewrite`，且不会读到新 `.env`。必须重启一次让新调度 + 新环境变量生效。

**安全规程**：
1. 通过计划任务重启（不要直接 taskkill 裸进程）：
   - `schtasks /End /TN "Dajian Scheduler Daemon"`
   - 确认旧 PID 已消失（进程列表查 13628 不在）——**mutex 必须释放，否则新实例会因 mutex 冲突自杀**
   - `schtasks /Run /TN "Dajian Scheduler Daemon"`
2. 注意 `Dajian Scheduler Watchdog` 计划任务可能同时想拉起实例——若看到 mutex 冲突日志，等 30–60s 让其中一个稳定持有 mutex 后再验证。
3. **逃生口（重要）**：若旧 PID 用 `schtasks /End` 后仍存活、且终止需要 UAC 提权 → 你无法完成交互式提权，**立即停止**，在 summary 写明"守护进程需用户手动在提权终端重启"并给出上面三条命令，把控制权交还用户。**不要卡在重试循环里。**

## 验证（不得触发真实 live apply）

重启后确认，全部只读、不 apply：
1. `logs/_scheduler.pid` 的 PID 已变（≠13628），该 PID 存活
2. `logs/scheduler_daemon.log` 尾部出现启动横幅 + 含 `12:30  语义改写闭环` 日志行 + `PID 锁已获取`
3. `python scheduler_daemon.py --status` 不报错（或日志显示调度表已配置）
4. **不要**用 `--apply` 对任何真实 SKU 试射来"验证双闸已开"——那会真的推送。双闸是否打开由 `.env` 键存在性保证即可，无需触发。

## 产出物

`logs/activation_p3_summary.md`：
- 操作 1：追加前后 `.env` 键数（20→21）、原凭证键完整性核对结果（**只列键名，不列值**）
- 操作 2：旧 PID / 新 PID、日志验证截取（含 12:30 行）、是否走了逃生口
- 明确声明：未改任何代码、未 git commit、未改 `.env` 中既有行、未对真实 SKU 试射 apply

## 自检清单

- [ ] 已确认本文件是授权例外，仅这两个操作被解禁，其余禁令仍在
- [ ] 已确认在任务书 #5 阶段 C 之后执行
- [ ] `.env` 只追加不重写，绝不打印值，凭证键数 20→21 且原键完整
- [ ] 守护进程走计划任务重启，遇提权需求即停并交还用户
- [ ] 验证阶段绝不 `--apply` 真实 SKU
