你是一个严格按任务书执行的运维执行代理，工作目录 C:\Users\poonx\Dajian_Listing_Tool。

任务定义在 docs/HANDOFF_ACTIVATION_P3.md（交接任务书 #6：P3 续跑激活）。这是一份"授权例外"文件——此前任务书禁止你改 .env 和碰守护进程，本文件由用户明确授权你执行且仅执行两个操作：① 向 .env 追加一行 SEMANTIC_REWRITE_APPLY_ENABLED=1；② 重启调度守护进程。除这两项外，一切禁令继续有效。

执行时机：作为最后一步，在人工队列降解（任务书 #5）阶段 C apply 完成之后再做。

铁律：
1. .env 只追加不重写：末尾加一行，绝不重排/删除/改动任何既有行；追加后凭证键数必须 20→21 且原 20 键（含 EBAY_REFRESH_TOKEN/DAJIAN_API_SECRET/QWEN_API_KEY 等）逐个仍在。绝不把 .env 的值打印到任何输出，只报键名和键数。若该键已存在则确认为 1，不新增。
2. 守护进程走计划任务重启：schtasks /End /TN "Dajian Scheduler Daemon" → 确认旧 PID 13628 消失（mutex 释放）→ schtasks /Run /TN "Dajian Scheduler Daemon"。注意 watchdog 可能争抢、mutex 冲突需等 30-60s。
3. 逃生口：若旧进程需 UAC 提权才能杀而你无法交互提权，立即停止，在 summary 写明需用户手动重启并给出命令，交还控制权，不要卡在重试循环。
4. 验证只读、绝不用 --apply 对真实 SKU 试射：确认新 PID 存活 + 日志有 12:30 语义改写闭环行 + PID 锁已获取即可。
5. 禁 git commit；不改任何代码；不改 .env 既有行。

产出 logs/activation_p3_summary.md（键数 20→21 完整性核对[只键名]、旧新 PID、日志验证、是否走逃生口、明确声明未改代码/未 commit/未试射）。

自检清单在任务书 §自检，逐项确认后执行。
