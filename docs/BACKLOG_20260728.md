# BACKLOG_20260728

本轮 QC 自动化修复已处理大部分可自动化的报错，遗留未处理的 57 条 CRITICAL 主要归入以下几类，由于性质需要人工介入或存在技术债，本轮不做修复（禁止绕过机制强制 live 写入）：

## 1. semantic_dimension (21 条)
* **原因/性质**: 审计判定此维度问题不可信或风险过高，未提供自动修复（实测 `fix_keys` 为空）。
* **本轮不修理由**: 需要人工判断是采用数据源、保留 live 数据，或是彻底存疑。

## 2. category_mismatch (17 条)
* **原因/性质**: 本地规则集匹配器与基于大模型/GIGA 数据的 `listing_quality_gate` 分类引擎对类目存在判定分歧（两套引擎口味不同）。
* **本轮不修理由**: 目前已被 `check_category_engine_drift.py` 漂移检测工具盯住（当前 23 条漂移记录中包含了这 17 条不匹配），留待引擎统一迭代解决。

## 3. semantic_material (12 条)
* **原因/性质**: 提取器产生了错位或真幻觉，误识别了不合理的材质。
* **本轮不修理由**: 需要详细指引（“改成什么”），这部分清单见 `logs/review_human18_final.csv`。本轮以不触碰不可预期的修改为底线，故不作处理。

## 4. semantic_capacity (4 条)
* **原因/性质**: "床面尺寸" (king size) 与 "坐卧人数" (4 seater/4 person) 的事实冲突判定题。模型依据部分词汇报警，无法做简单的查找替换。
* **本轮不修理由**: 需业务人工决断，无法自动化兜底。

## 5. 颜色/表面处理误判为材质
* **原因/性质**: 材质提取器错误地把 "glossy", "green" 等颜色或工艺词视作 `material`。
* **本轮不修理由**: 属于抽取器的技术债，需从源头（大模型提取或提取规则库）解决，业务层面的强行抹除不能根治问题。
